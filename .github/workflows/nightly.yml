name: nightly

on:
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  performance:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.22.2
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          NEXT_PUBLIC_SITE_URL: https://calculandia.ru
      - run: npx lhci autorun --config=.lighthouserc.nightly.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: lighthouse-${{ github.run_id }}
          path: artifacts/lighthouse
          retention-days: 30

  full-audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.22.2
          cache: npm
      - run: npm ci
      # Тот же прод-гейт, что в ci/release: high/critical в прод-зависимостях
      # блокируют (allowlist с expiry — scripts/audit-prod.mjs). Сырой
      # `npm audit` валил nightly dev-цепочкой @lhci→puppeteer→extract-zip
      # (GHSA-jmr9-qjv8-65gv, исправленной версии нет), которая в прод не
      # попадает: она живёт только в CI-задаче замера Lighthouse.
      - run: npm run audit:prod
