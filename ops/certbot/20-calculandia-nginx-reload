#!/usr/bin/env bash
set -Eeuo pipefail

if [[ ${EUID} -ne 0 ]]; then
  echo "certbot deploy hook must run as root" >&2
  exit 1
fi

nginx -t
systemctl reload nginx
echo "nginx reloaded after successful certificate renewal"
